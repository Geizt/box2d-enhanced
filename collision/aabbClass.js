// Generated by CoffeeScript 1.3.3
(function() {

  ({
    box2d: (typeof exports !== "undefined" && exports !== null) && exports || this.box2d || (this.box2d = {})
  });

  box2d.AABB = (function() {

    function AABB(minVertex, maxVertex) {
      if (minVertex != null) {
        if (!(typeof minVertex.IsValid === "function" ? minVertex.IsValid() : void 0)) {
          throw new TypeError;
        }
      }
      if (maxVertex != null) {
        if (!(typeof maxVertex.IsValid === "function" ? maxVertex.IsValid() : void 0)) {
          throw new TypeError;
        }
      }
      this.minVertex = new box2d.Vector2D();
      this.maxVertex = new box2d.Vector2D();
      if ((minVertex != null) && (maxVertex != null)) {
        this.minVertex.SetV(minVertex);
        this.maxVertex.SetV(maxVertex);
      }
    }

    AABB.prototype.IsValid = function() {
      var dX, dY, valid;
      dX = this.maxVertex.x;
      dY = this.maxVertex.y;
      dX -= this.minVertex.x;
      dY -= this.minVertex.y;
      valid = dX >= 0.0 && dY >= 0.0;
      return valid && this.minVertex.IsValid() && this.maxVertex.IsValid();
    };

    return AABB;

  })();

}).call(this);
